# .env.production.example
# Copie este arquivo como `.env.production` em seu servidor/provedor (NUNCA commite `.env.production` com segredos reais)

# Banco de dados (pontos de atenção: use RDS endpoint ou host privado)
DB_HOST=<RDS_HOST_OR_IP>
DB_PORT=3306
DB_USER=<DB_MIGRATE_USER>
DB_PASSWORD=<DB_MIGRATE_PASS>
DB_NAME=<DB_NAME>

# App
NODE_ENV=production
PORT=3000

# JWT - Gere secrets fortes (veja instruções abaixo)
JWT_SECRET=<GENERATE_A_STRONG_SECRET>
JWT_REFRESH_SECRET=<GENERATE_A_STRONG_REFRESH_SECRET>
JWT_EXP=1h
JWT_REFRESH_EXP=7d

# CORS (ajuste para seu domínio de frontend)
ALLOWED_ORIGINS=https://seu-frontend.example.com

# Opcional: para o script de re-hash usar contas inativas (default = active only)
# Defina REHASH_ONLY_ACTIVE=false para re-hashar todos os usuários
REHASH_ONLY_ACTIVE=true

# -----------------------------
# Como gerar secrets fortes (exemplos):
# 1) Usando OpenSSL:
#    openssl rand -base64 48
# 2) Usando Node.js:
#    node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Cole o valor gerado em JWT_SECRET e em JWT_REFRESH_SECRET (valores diferentes)
# -----------------------------
